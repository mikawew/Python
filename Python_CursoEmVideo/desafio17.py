catopst = float(input('Comprimento do cateto oposto: '))

catadj = float(input('Comprimento do cateto adjacente: '))

#A hiportenusa é a raiz quadrada da soma dos quadrados dos catetos

Tot = (catopst ** 2 + catadj ** 2) ** (1/2)
